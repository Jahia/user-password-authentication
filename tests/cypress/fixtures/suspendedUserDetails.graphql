query suspendedUserDetails($username: String!) {
    admin {
        userAdmin {
            user(username: $username) {
                node {
                    mixinTypes(
                        fieldFilter: {filters: [{fieldName: "name", value: "upa:mfaSuspendedUser"}]}
                    ) {
                        name
                    }
                }
                property(name: "upa:mfaSuspendedSince")
            }
        }
    }
}
