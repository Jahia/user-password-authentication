mutation verifyEmailCodeFactor($code: String!) {
    mfa {
        factors {
            verifyEmailCodeFactor(code: $code) {
                success
                error
                sessionState
                requiredFactors
                completedFactors
            }
        }
    }
}
