mutation verifyEmailCodeFactor($code: String!) {
    upa {
        mfaFactors {
            emailCode {
                verify(code: $code) {
                    session {
                        error {
                            code
                            arguments {
                                name
                                value
                            }
                        }
                        verifiedFactors
                        requiredFactors
                        factorState(factorType: "email_code") {
                            verified
                            error {
                                code
                                arguments {
                                    name
                                    value
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
