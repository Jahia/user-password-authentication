mutation verifyEmailCodeFactor($code: String!) {
    upa {
        mfaFactors {
            emailCode {
                verify(code: $code) {
                    session {
                        error {
                            code
                            arguments {
                                name
                                value
                            }
                        }
                        requiredFactors
                        verifiedFactors
                        remainingFactors
                        factorState(factorType: "email_code") {
                            verified
                            error {
                                code
                                arguments {
                                    name
                                    value
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
