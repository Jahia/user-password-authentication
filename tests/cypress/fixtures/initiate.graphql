mutation initiate($username: String!, $password: String!, $site: String) {
    mfa {
        initiate(username: $username, password: $password, site: $site) {
            success
            error
            sessionState
            requiredFactors
            completedFactors
        }
    }
}
