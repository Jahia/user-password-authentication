mutation prepareFactor($factorType: String!) {
    mfa {
        prepareFactor(factorType: $factorType) {
            success
            error
            sessionState
            requiredFactors
            completedFactors
        }
    }
}
