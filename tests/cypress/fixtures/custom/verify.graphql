mutation verifyCustomFactor($number: Int!) {
    mfa {
        factors {
            custom {
                verify(number: $number) {
                    session {
                        error {
                            code
                            arguments {
                                name
                                value
                            }
                        }
                        completedFactors
                        requiredFactors
                        factorState(factorType: "custom") {
                            verified
                            error {
                                code
                                arguments {
                                    name
                                    value
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
